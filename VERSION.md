# 版本说明

## 1.0

- 初始版本：提供网页监控可视化看板。
- 默认监听目标：`http://129.226.153.254:5078`。
- 本项目默认端口：`5079`。
- 支持展示健康状态、系统资源、业务任务、API 指标和中文 JSON 指标说明。
- Docker 镜像由 GitHub Action 自动构建并推送。
- 同一个镜像会同时打上 `1.0` 和 `latest` 两个标签。
- 本地不执行任何 Docker 镜像推送操作。
- `.dockerignore` 保持排除 `.env`，避免环境变量文件进入镜像构建上下文。
